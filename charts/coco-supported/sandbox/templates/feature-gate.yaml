apiVersion: v1
kind: ConfigMap
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  name: osc-feature-gates
  namespace: openshift-sandboxed-containers-operator
data:
  confidential: "true"
  {{ if eq .Values.clusterGroup.name "baremetal" }}
  layeredImageDeployment: "true"
  {{- else }}
  confidential: "true"
  {{- end }}
