apiVersion: v1
kind: Service
metadata:
  name: insecure-policy
spec:
  ports:
  - name: 8080-tcp
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: insecure-policy
  sessionAffinity: None
  type: ClusterIP
